<%- include("header") -%>
    <div class="box" id="heading">
        <h1>
            <%= listTitle %>
        </h1>    
    </div>
    
    <div class="box">
        <!-- <% for(var i=0; i<newItemsList.length; i++){ %>
            <div class="item">
                <input type="checkbox">
                <p> <%= newItemsList[i].name %> </p>
            </div>
        <% } %> -->

        <% newItemsList.forEach(function(i){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= i._id %>" onChange="this.form.submit()">
                    <p> <%= i.name %> </p>
                </div>
                <input type="hidden" name="listName" value="<%= listTitle %>">
            </form>
        <% }) %>

        <form action="/" method="post" class="item">
            <input type="text" name="newItem" placeholder="Add New Item" autocomplete="off">
            <button type="submit" name="button" value="<%= listTitle %>">+</button>
        </form>

    </div>
<%- include("footer") -%>   

